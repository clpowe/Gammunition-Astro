---
const { price, time, description, info } = Astro.props;
---

<div class="trainingCard items-center flex flex-col max-w-md lg:flex-row gap-2 p-5 mx-auto bg-gray-400 rounded-md bg-opacity-10">
  <div class="flex flex-col justify-center items-center flex-grow-0 self-auto lg:self-start">
    <div class="text-white font-medium flex-grow-0 text-8xl">${ price }</div>
    <div class="text-accent uppercase text-xl tracking-wider mt-2">per person</div>
  </div>
  <div class="lg:text-left text-center space-y-1 flex-grow-0 max-w-xs md:max-w-md">
    <div class="text-white">{time}</div>
    <p class="text-secondary-light text-body  tracking-wider font-roboto">{description}</p>
    <div class="text-accent uppercase">{info}</div>
  </div>
</div>

<style lang="scss">

</style>